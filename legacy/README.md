# Legacy Scripts

This folder contains earlier research scripts retained for traceability.

The supported installable package lives under `src/edrft`. Code in this folder
is not included in the PyPI wheel and should not be treated as public API.

Where practical, old `rft` and `edrft` script filenames have been renamed to
`rft` and `edrft` to match the model and package naming.
